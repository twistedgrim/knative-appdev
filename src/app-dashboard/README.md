# app-dashboard

Simple Knative dashboard service that lists running Knative services from Kubernetes API.

## Deploy
From repo root:

```bash
./scripts/deploy-app-dashboard.sh
```

Then open:
- `http://app-dashboard.platform-system.localhost:8081`
